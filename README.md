# ECHO: Estimation of Cryptic Hosts from Outbreak trees

This package implements ECHO, a collection of three estimators of the number of unsampled cases related to an observed phylogenetic tree.

The package can be installed by running `remotes::install_github("https://github.com/MAGPIE-SFU/echo")`.
